# ListContainersOptions.ListContainersInclude

Datasets which may be included as part of the call response.

``` swift
public enum ListContainersInclude
```

## Inheritance

`String`

## Enumeration Cases

### `metadata`

Include the containers' metadata in the response.

``` swift
case metadata
```
