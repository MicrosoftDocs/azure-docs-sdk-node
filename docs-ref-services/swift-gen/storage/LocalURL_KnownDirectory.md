# LocalURL.KnownDirectory

Well-known directories that are not part of the existing `FileManager.SearchPathDirectory` enum.

``` swift
public enum KnownDirectory
```

## Inheritance

`String`

## Enumeration Cases

### `tempDirectory`

The application's temporary directory.

``` swift
case tempDirectory
```
