### YamlMime:TSTypeAlias
name: ClientEventType
uid: '@azure/ai-voicelive.ClientEventType'
package: '@azure/ai-voicelive'
summary: >-
  Client event types used in VoiceLive protocol. \

  [KnownClientEventType](xref:@azure/ai-voicelive.KnownClientEventType) can be
  used interchangeably with ClientEventType,
   this enum contains the known values that the service supports.
  ### Known values supported by the service

  **session.update** \

  **input_audio_buffer.append** \

  **input_audio_buffer.commit** \

  **input_audio_buffer.clear** \

  **input_audio.turn.start** \

  **input_audio.turn.append** \

  **input_audio.turn.end** \

  **input_audio.turn.cancel** \

  **input_audio.clear** \

  **conversation.item.create** \

  **conversation.item.retrieve** \

  **conversation.item.truncate** \

  **conversation.item.delete** \

  **response.create** \

  **response.cancel** \

  **session.avatar.connect** \

  **mcp_approval_response** \

  **output_audio_buffer.clear**: Client request to clear the avatar output
  buffer. \

  **input_text.delta**: Streamed delta of input text content being appended to
  an item. \

  **input_text.done**: Signals that the streamed input text content for an item
  is complete.
fullName: ClientEventType
remarks: ''
isDeprecated: false
syntax: |
  type ClientEventType = string
