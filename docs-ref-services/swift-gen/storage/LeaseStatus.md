# LeaseStatus

The status of a lease on a blob or container.

``` swift
public enum LeaseStatus
```

## Inheritance

`Codable`, `String`

## Enumeration Cases

### `locked`

The lease is locked.

``` swift
case locked
```

### `unlocked`

The lease is unlocked.

``` swift
case unlocked
```
